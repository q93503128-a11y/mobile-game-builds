# 모바일 게임 공통 개발 규칙

이 문서는 `mobile-game-builds` 전체 프로젝트에 적용되는 공통 규칙이다. 프로젝트별 문서는 이 규칙을 강화할 수 있지만 임의로 약화할 수 없다.

## 1. 저장소와 정본

- 기본 브랜치는 `main`이다.
- 실제 현재 코드와 데이터가 최우선 정본이다.
- 각 프로젝트는 `docs/DESIGN_BIBLE.md`를 기획 정본으로 유지한다.
- 기능·밸런스·경제·UI·서버 규칙을 변경하면 관련 정본 문서를 같은 작업에서 갱신한다.
- 과거 채팅, 인계문, 오래된 TODO, 주석은 현재 `main`보다 우선하지 않는다.
- 다른 프로젝트 코드를 무단으로 복사해 결합하지 않는다. 공용화 가치가 검증된 코드만 `shared/`로 이동한다.

## 2. 코드 위생 — 쓰레기 코드 누적 금지

다음 항목은 “나중에 청소할 것”으로 미루지 않고, 관련 작업 안에서 즉시 처리한다.

- 더 이상 호출되지 않는 죽은 코드
- 동일 기능을 중복 구현한 함수·클래스·데이터
- 임시 우회 로직, 임시 플래그, 디버그 전용 분기
- 교체된 레거시 구현
- 의미 없는 래퍼, 불필요한 추상화, 한 번도 재사용되지 않는 범용화
- 사용하지 않는 변수·상수·리소스·에셋 참조
- 오래된 TODO/FIXME 중 이미 결정이 끝난 항목
- 이름만 다르고 같은 데이터를 두 군데에서 유지하는 이중 정본
- 실패를 숨기는 빈 catch, 무조건 성공 처리, 오류 삼키기
- 실제 제품 규칙을 테스트 안에서만 맞추는 하드코딩

### 2.1 작업 종료 전 필수 점검

기능 하나를 끝낼 때 최소한 다음을 확인한다.

1. 기존 구현을 대체했다면 구 구현을 제거했는가?
2. 동일 계산식/상수가 여러 곳에 복제되지 않았는가?
3. 실패 경로가 정상적으로 보고되는가?
4. 새 코드가 기존 규칙과 충돌하지 않는가?
5. 관련 테스트와 문서를 함께 수정했는가?
6. 임시 파일·로그·덤프·백업이 저장소에 남지 않았는가?

## 3. 아키텍처 원칙

- 클라이언트, 서버, 정적 데이터, 도구, 테스트의 책임을 분리한다.
- 서버 권위가 필요한 값은 클라이언트가 최종 결정하지 않는다.
- 결제, 재화, 가챠 결과, 계정 진행도, 랭킹 점수, 길드 상태, 채팅 권한, 보상 수령 여부는 서버 검증 대상으로 본다.
- 클라이언트의 표시용 캐시와 서버 정본을 구분한다.
- 중요한 경제 변화에는 idempotency/중복 수령 방지 전략을 둔다.
- 저장 데이터에는 버전 필드를 두고 마이그레이션 가능하게 설계한다.

## 4. 모바일 우선

- PC 조작을 그대로 축소 이식하지 않는다.
- 기본 입력은 터치와 한 손 플레이 가능성을 우선 검토한다.
- 버튼의 터치 영역, 작은 화면 가독성, 노치/세이프에어리어, 다양한 비율을 고려한다.
- 전투 가독성을 위해 적·투사체·VFX의 수보다 우선순위와 실루엣을 관리한다.
- 성능 목표를 정하고 저사양 안드로이드에서도 프레임 급락이 누적되지 않게 한다.
- 배터리·발열·메모리·백그라운드 복귀를 PC보다 중요하게 다룬다.

## 5. 밸런스와 벤치마크

- 공격력, 체력, 성장비용, 뽑기 확률, 천장, 재화 공급량, 던전 입장량 등을 감으로 확정하지 않는다.
- Capybara Go, Survivor.io, Archero 2 등 검증된 동종 게임의 실제 구조와 수치 범위를 조사하고 비교한다.
- 외부 게임의 특정 수치를 그대로 복제하지 않는다. 여러 사례의 범위와 의도를 분석한 뒤 현재 게임의 전투 길이, 조작, 성장 속도에 맞게 변형한다.
- 외부 자료는 출처, 관측 시점, 확실도, 우리 게임에 적용한 이유를 기록한다.
- 과금 유도는 강하게 설계할 수 있으나, 상품 가치와 확률·천장·보상 규칙은 명확하고 검증 가능해야 한다.

## 6. 콘텐츠와 과금

- 메인 스테이지, 장비/부품, 가챠, 성장, 던전, 랭킹, 길드, 이벤트는 서로 재화를 순환시키는 구조로 설계한다.
- 단일 성장축만 끝없이 수치화하지 않고 콘텐츠별 빌드 선택 이유를 만든다.
- 한정 상품이나 경쟁 콘텐츠가 코어 전투 자체를 무의미하게 만들지 않게 한다.
- 동일 아이템 중복 획득은 의미 있는 승급·재료·교환 가치로 연결한다.
- 신규 아이템 획득 후 기존 강화 투자가 통째로 손실되는 구조는 지양한다.

## 7. 디자인과 외부 게임 참고

- 외부 게임에서 가져오는 것은 검증된 구조, 흐름, 경제 원리, UX 패턴이다.
- 이름, 캐릭터, 일러스트, 아이콘, 애니메이션, 문구, 맵, 고유 수치표를 그대로 복제하지 않는다.
- UI는 정보 위계와 상호작용 패턴을 참고하되 프로젝트의 시각 언어로 다시 설계한다.
- 참고한 요소는 프로젝트의 `docs/BENCHMARKS.md`에 기록한다.

## 8. 테스트와 검증

- 자동 테스트만 통과했다고 완료 처리하지 않는다.
- 핵심 기능은 수동 코드 검토와 실제 플레이 흐름 검토를 같이 한다.
- 밸런스 데이터에는 최소한 범위 검증, 중복 ID, 누락 참조, 확률 합, 보상 음수/오버플로 검사를 둔다.
- 모바일 UI는 대표 해상도와 화면비에서 확인한다.
- 서버 API는 재시도, 중복 요청, 네트워크 중단, 오래된 클라이언트 요청을 고려한다.

## 9. 성능과 기술 부채

- 성능 문제를 기능 완료 후 한꺼번에 해결하는 방식으로 미루지 않는다.
- 프레임마다 할당되는 객체, 무제한 리스트 성장, 이벤트 리스너 누수, 해제되지 않는 타이머/코루틴을 금지한다.
- 반복 생성되는 투사체·적·VFX는 풀링 등 적절한 재사용 구조를 고려한다.
- 프로파일링 없이 “최적화했다”고 기록하지 않는다.
- 기술 부채를 만들 수밖에 없는 경우 이유, 제거 조건, 기한을 명시한 이슈 또는 문서를 남긴다.

## 10. 완료 기준

한 작업은 다음 조건을 모두 만족해야 완료다.

- 요구 기능이 실제 동작함
- 관련 회귀가 없음
- 중복·죽은·임시 코드가 남지 않음
- 관련 데이터 검증 완료
- 관련 문서 동기화
- 필요한 테스트 추가/갱신
- 사용자에게 보이는 디버그/개발자 문구 제거
- 다음 작업자가 현재 구조를 `main`만 보고 재구성할 수 있음
